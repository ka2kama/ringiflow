# ヘルスチェック API テスト
# GET /health
#
# サービスの稼働状態を確認する。認証不要。

# =============================================================================
# 正常系: サービスが稼働している
# =============================================================================
# Given: BFF サービスが起動している
# When: GET /health にリクエストする
# Then: "healthy" ステータスとバージョン情報が返される

GET {{bff_url}}/health

HTTP 200
[Asserts]
jsonpath "$.status" == "healthy"
jsonpath "$.version" exists
