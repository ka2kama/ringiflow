# SSO（OIDC）機能仕様書・詳細設計の作成

## コンテキスト

### 目的
- Issue: #1119
- Want: SSO（OIDC）認証の実装に先立ち、機能仕様と技術設計を確立する（Epic #1118 の設計基盤）
- 完了基準:
  - 機能仕様書に SSO（OIDC）のユーザー操作フローを記述する
  - 詳細設計書に OIDC 認証フローの技術設計を記述する
  - OpenAPI 仕様書を更新する（新規エンドポイント追加）— ※ openapi.yaml は utoipa からの生成物のため、API 仕様は詳細設計書で定義し、実際の openapi.yaml 更新は実装 Story で対応

### ブランチ / PR
- ブランチ: `feature/1119-sso-oidc-spec-design`
- PR: #1132（Draft）

### As-Is（探索結果の要約）
- 機能仕様書: `docs/20_機能仕様書/` に SSO 関連なし。テンプレートは `00_はじめに.md` に定義（概要→シナリオ→画面・操作フロー→機能詳細→状態遷移→権限→非ゴール→未解決事項→関連ドキュメント）
- 詳細設計書: `docs/40_詳細設計書/07_認証機能設計.md` は Phase 1（メール/パスワード）のみ。`08_AuthService設計.md` は Phase 2 の Auth Service 分離。SSO は両方で「Phase 3 以降」として明示的にスコープ外。最新の連番は 17
- OpenAPI: `openapi/openapi.yaml` に認証 API（login, logout, me, csrf）は定義済み。OIDC エンドポイントは未定義
- 要件定義書: AUTH-003（OIDC + PKCE）、AUTH-010（トークン秘匿）が定義済み。OIDC フロー図は `01_コア要件.md` 4.1 節にスケッチ済み
- Auth Service: `08_AuthService設計.md` に「将来の SSO/MFA」が Auth Service の責務と記載
- DB: `users` テーブルに `password_hash` フィールドあり（Phase 2 で credentials テーブルに分離予定）。`auth` スキーマが Phase 2 で導入予定
- ロードマップ: Phase 3-1 が SSO（OIDC）。Epic #1118 のタスクとして本 Issue が最初の Story

### 進捗
- [x] Phase 1: 機能仕様書作成
- [x] Phase 2: 詳細設計書作成（API 仕様 = OpenAPI 相当の定義を含む）

---

## 仕様整理

### スコープ
- 対象:
  - 機能仕様書: SSO（OIDC）のユーザー操作フロー（SSO ログイン、OIDC プロバイダー設定管理）
  - 詳細設計書: OIDC 認証フローの技術設計（API、DB スキーマ、JIT プロビジョニング、セキュリティ）
  - OpenAPI 仕様書: OIDC 認証エンドポイント + OIDC プロバイダー管理 API
- 対象外:
  - SAML 2.0 連携（Phase 3 の後続サブフェーズ）
  - MFA（Phase 3 の後続サブフェーズ）
  - 実際のコード実装（後続 Story #1120-#1123 で対応）

### 操作パス

操作パス: 該当なし（ドキュメント作成のみ。操作パスは機能仕様書内で記述される）

---

## 設計

### 設計判断

| # | 判断 | 選択肢 | 選定理由 | 状態 |
|---|------|--------|---------|------|
| 1 | 詳細設計書のファイル構成 | A: 既存 07 を拡張 / B: 新規ファイルを作成 | B を選択。07 は Phase 1 スコープで完結しており 632 行。SSO は OIDC プロトコル・IdP 連携・JIT プロビジョニングなど独立性の高い関心事を含む。08_AuthService設計.md も同様に分離されている | 確定 |
| 2 | 詳細設計書の番号 | 18 | 17 が最新の連番 | 確定 |
| 3 | 機能仕様書の番号 | 07 | 06 が最新の連番 | 確定 |
| 4 | OIDC トークン交換の責務 | A: BFF / B: Auth Service | B を選択。08_AuthService設計.md に「将来の SSO/MFA」が Auth Service の責務と記載。client_secret の管理も Auth Service が適切 | 確定 |
| 5 | OIDC プロバイダー設定の保存先 | A: public スキーマ / B: auth スキーマ | B を選択。OIDC プロバイダー設定は認証情報（client_secret 等）を含むため、Auth Service が所有する auth スキーマに配置する | 確定 |

### Phase 1: 機能仕様書作成

`docs/20_機能仕様書/07_SSO認証.md` を作成する。

#### 確認事項
- パターン: 機能仕様書テンプレート → `docs/20_機能仕様書/00_はじめに.md`
- パターン: 既存の機能仕様書（参照例）→ `docs/20_機能仕様書/02_ユーザー管理.md`
- 型: ユーザーロール定義 → `docs/10_要件定義書/01_コア要件.md`（ロール別権限）
- 型: 画面一覧 → `docs/10_要件定義書/01_コア要件.md` SCR-001, SCR-011

#### 内容構成
1. 概要: SSO（OIDC）の目的、対象ユーザー、関連要件（AUTH-003, AUTH-010）
2. シナリオ: SSO ログイン、OIDC プロバイダー設定、JIT プロビジョニング
3. 画面・操作フロー: SSO ログインフロー、OIDC 設定管理フロー
4. 機能詳細: ログイン画面の SSO 選択、OIDC プロバイダー設定画面
5. 状態遷移: OIDC プロバイダーの状態（有効/無効）
6. 権限: ロール別操作権限
7. 非ゴール: SAML, MFA, SCIM
8. 未解決事項
9. 関連ドキュメント

#### テストリスト

ユニットテスト（該当なし）
ハンドラテスト（該当なし）
API テスト（該当なし）
E2E テスト（該当なし）

（ドキュメント作成のみの Issue）

### Phase 2: 詳細設計書作成

`docs/40_詳細設計書/18_SSO認証設計.md` を作成する。

#### 確認事項
- パターン: 既存の認証設計書 → `docs/40_詳細設計書/07_認証機能設計.md`
- パターン: Auth Service 設計 → `docs/40_詳細設計書/08_AuthService設計.md`
- 型: セッション管理の構造 → `docs/40_詳細設計書/07_認証機能設計.md` SessionData 構造
- 型: DB スキーマ設計パターン → `docs/40_詳細設計書/02_データベース設計.md`
- 型: API 設計方針 → `docs/40_詳細設計書/03_API設計.md`
- 型: ID 設計規約 → `docs/40_詳細設計書/04_ID設計規約.md`

#### 内容構成
1. 概要: 対象要件、Phase 3-1 スコープ
2. アーキテクチャ: OIDC 認証フロー全体像（シーケンス図）、責務分担
3. API 設計:
   - BFF 公開 API: GET /api/v1/auth/oidc/start, GET /api/v1/auth/oidc/callback
   - BFF 公開 API（管理者）: OIDC プロバイダー CRUD
   - Auth Service 内部 API: OIDC 認証処理
   - Core Service 内部 API: JIT プロビジョニング
4. データベース設計: oidc_providers テーブル（auth スキーマ）
5. セキュリティ考慮事項: state パラメータ、PKCE、トークン秘匿、client_secret 暗号化
6. JIT プロビジョニング設計
7. 実装コンポーネント: ファイル構成、インターフェース定義
8. テスト計画
9. 変更履歴

### Phase 3: OpenAPI 仕様の定義（詳細設計書内）

`openapi/openapi.yaml` は utoipa アノテーションからの生成物のため、直接編集しない。
詳細設計書（Phase 2）の API 設計セクションで OpenAPI 相当の仕様（パス、リクエスト/レスポンススキーマ、エラーコード）を定義する。
実際の openapi.yaml 更新は実装 Story（#1120, #1121）で utoipa アノテーションを書いた後に `just openapi-generate` で生成する。

#### 確認事項: なし（Phase 2 の API 設計セクションで対応済み）

---

## ブラッシュアップ

### ギャップ発見の観点 進行状態

| 観点 | 状態 | メモ |
|------|------|------|
| 未定義 | 完了 | JIT プロビジョニング時のデフォルトロール、OIDC state/PKCE の Redis キー設計、テナントごとの provider 数制限を定義 |
| 曖昧 | 完了 | OIDC プロバイダー管理のスコープ（CRUD + テスト接続は対象外）、JIT 時の照合方法（email + tenant_id）を明確化 |
| 競合・エッジケース | 完了 | password + OIDC 両方の認証、プロバイダー無効化後のログイン試行、ID トークンの email が inactive ユーザーと一致する場合を検討 |
| 不完全なパス | 完了 | OIDC フロー失敗時のエラーハンドリング（IdP 拒否、ネットワークエラー、無効な state）を追加 |
| アーキテクチャ不整合 | 完了 | credentials テーブル（credential_type=oidc）と oidc_providers テーブルの関係を整理。Auth Service → Core Service の JIT 呼び出し方向を確認 |
| 既存手段の見落とし | 完了 | セッション管理（07）と CSRF 防御は SSO ログイン後も既存のものをそのまま流用 |

### ループの記録

| ループ | 検出したギャップ | 観点 | 対応 |
|-------|----------------|------|------|
| 1回目 | OpenAPI が生成物であることが完了基準と矛盾 | アーキテクチャ不整合 | Phase 3 を「詳細設計書内で API 仕様を定義」に変更。実際の openapi.yaml 更新は実装 Story に委ねる |
| 1回目 | auth.credentials テーブルに credential_type=oidc が既に予定されている | 既存手段の見落とし | 新テーブルではなく既存設計を活用。oidc_providers テーブルのみ新規追加 |
| 1回目 | JIT プロビジョニング時のデフォルトロール未定義 | 未定義 | テナントのデフォルトロール（user）を割り当てる設計を追加 |
| 1回目 | password + OIDC 両方の認証可否が曖昧 | 競合・エッジケース | 両方の認証方式を許容する設計（credentials テーブルに複数行）を明確化 |
| 1回目 | OIDC state/PKCE の一時データ保存先未定義 | 未定義 | Redis に TTL 付きで保存する設計を追加（キー: `oidc_state:{state}`) |

### 未解決の問い
- なし

---

## 収束確認（設計・計画）

| # | 観点 | 理想状態（To-Be） | 判定 | 確認内容 |
|---|------|------------------|------|---------|
| 1 | 網羅性 | 全対象が計画に含まれている | OK | 機能仕様書、詳細設計書、OpenAPI（設計書内で定義）の3成果物を網羅 |
| 2 | 曖昧さ排除 | 不確定な記述がゼロ | OK | OIDC プロバイダー管理スコープ、JIT 照合方法、認証方式の共存を明確化 |
| 3 | 設計判断の完結性 | 全ての差異に判断が記載 | OK | ファイル構成、番号、責務分担、保存先の5判断が確定 |
| 4 | スコープ境界 | 対象と対象外が両方明記 | OK | 対象（OIDC）と対象外（SAML, MFA, SCIM, 実装）を明記 |
| 5 | 技術的前提 | 前提が考慮されている | OK | OpenAPI 生成物制約、Auth Service 責務、credentials テーブル設計を確認 |
| 6 | 既存ドキュメント整合 | 矛盾がない | OK | 07_認証機能設計、08_AuthService設計、02_データベース設計、実装ロードマップとの整合を確認 |
