# #1055 レビュープロンプト観点5の記述を簡潔化する

## コンテキスト
- Issue: #1055
- PR: #1056
- ブランチ: `feature/1055-simplify-review-prompt-observation5`

## 背景

PR #1054 で DI パターン一貫性チェックを観点5に追加した際、括弧内に詳細な説明を含めたため、観点5の記述が他の観点（各1文程度）と比較して長くなった。PR #1054 の Verification レビューで Low 指摘として検出された。

## 変更内容

`claude-auto-review.yaml` L258 の観点5の DI パターン詳細を `.claude/rules/rust.md` の DI コンポジションルートセクションへの参照に置き換えた。

変更前:
```
DI パターンの一貫性（外部クライアントの具象型構築は `main.rs` で行い `build_app` には `Arc<dyn Trait>` のみ渡す。`Option<具象型>` + `.expect()` パターンの混入を検出）
```

変更後:
```
DI パターンの一貫性（`.claude/rules/rust.md` の DI コンポジションルートセクション参照）
```

## 判断ログ

| 判断 | 選択肢 | 選定理由 |
|------|--------|---------|
| 詳細の配置先 | ルールファイルへの参照（A） / 観点5内で要約（B） | A: L265 の注記で「ルール準拠チェックは別ワークフローで実施」と既にルールファイル参照パターンが確立されている。`rust.md` に詳細が記載済みのため重複を避ける |
