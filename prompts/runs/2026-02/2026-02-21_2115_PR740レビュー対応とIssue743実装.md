# PR #740 レビュー対応と Issue #743 実装

## 概要

PR #740（Issue #726: ワークフローデザイナー接続線・プロパティ・バリデーション）のレビュー対応とマージ、および派生した Issue #743（WorkflowDefinitionDesignerNew ルート削除）の実装を行った。

## 実施内容

### PR #740 レビュー対応

Claude Auto Review の指摘 2 件に対応した。

1. High: グローバル KeyDown ハンドラが INPUT/TEXTAREA/SELECT 要素のイベントをキャプチャする問題
   - `Designer.elm` の `subscriptions` で、`Decode.at ["target", "tagName"]` を使用してフォーム要素からのキーイベントを `Decode.fail` でフィルタリングする修正を実施
2. Medium: `WorkflowDefinitionDesignerNew` ルートが空文字列の `definitionId` で `Designer.init` を呼び出す問題
   - Issue #743 として延期

修正後、PR #740 を squash merge した。

### Issue #743 実装

`WorkflowDefinitionDesignerNew` ルートと関連コードを 4 ファイルから削除した。

- `Route.elm`: バリアント、パーサー、toString、isRouteActive、pageTitle から削除
- `Main.elm`: init ケースとサイドバーナビアイテムを削除
- `Icons.elm`: `workflowDesigner` アイコン関数を削除
- `RouteTest.elm`: 関連テスト 2 件を削除（443 → 443 テスト通過）

## 判断ログ

- PR #740 レビュー: High 指摘（KeyDown ハンドラ）は即座に修正、Medium 指摘（空 definitionId）は Issue #743 に延期と判断した。延期理由はルート自体の必要性を見直す方が適切なため
- Issue #743: ワークフロー定義一覧画面から新規作成する正規フローが存在するため、直接 URL アクセス用のルートは不要と判断し、削除を選択した

## 成果物

### コミット

- `#743 Remove WorkflowDefinitionDesignerNew route and related code`（4 ファイル変更、+2/-52）

### PR

- PR #753: https://github.com/ka2kama/ringiflow/pull/753 （Draft）

### Issue

- Issue #743 作成（PR #740 のレビュー指摘から派生）
