procs:
  build:
    shell: "just dev-build"
  bff:
    shell: "just dev-run bff"
  core-service:
    shell: "just dev-run core-service"
  auth-service:
    shell: "just dev-run auth-service"
  web:
    shell: "just dev-web"
