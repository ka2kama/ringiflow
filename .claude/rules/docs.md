---
paths:
  - "**/*.md"
---

# ドキュメント規約

全ての Markdown ファイルに適用する。

## 共通ルール

| 項目 | ルール |
|------|--------|
| 図表 | Mermaid 記法を使用（ASCII art 禁止） |
| ノードラベル | `ID["ラベル"]` 形式（例: `A["日本語ラベル"]`） |
| 書式 | 太字・斜体は本当に必要な場合のみ |

## ファイル命名

| 対象 | ルール |
|------|--------|
| `docs/**` | 連番プレフィックス: `00_`, `01_`, ... |
| `prompts/runs/**` | `YYYY-MM-DD_NN_<トピック>.md` |
| その他 | 自由（ただし kebab-case 推奨） |

## 外国語名の表記

発音が自明でない固有名詞や、展開形が分かりにくい略語には、初出時に読みや展開形を併記する。

**併記すべき:**

| パターン | 例 |
|---------|-----|
| 発音が自明でない名前 | Vite（ヴィート）、Elm（エルム）、Cargo（カーゴ） |
| 展開形が分かりにくい略語 | BFF（Backend for Frontend） |

**不要:**

広く知られているもの（GitHub、API、REST、npm など）は併記不要。
