# RingiFlow 本番環境変数テンプレート
#
# このファイルをコピーして .env を作成し、実際の値を設定してください。
#   cp .env.example .env
#   vim .env

# ==========================================================
# PostgreSQL 設定
# ==========================================================
POSTGRES_USER=ringiflow
# 生成例: openssl rand -base64 32
POSTGRES_PASSWORD=your-strong-postgres-password-here
POSTGRES_DB=ringiflow_prod

# ==========================================================
# Redis 設定
# ==========================================================
# 生成例: openssl rand -base64 32
REDIS_PASSWORD=your-strong-redis-password-here

# ==========================================================
# アプリケーション設定
# ==========================================================

# ログレベル（trace, debug, info, warn, error）
RUST_LOG=info

# ==========================================================
# DevAuth 設定（開発用認証バイパス）
# ==========================================================

# デモ環境では true に設定すると、BFF 起動時に開発用セッションが自動作成される
# ログインページが未実装のため、デモ環境では DevAuth を使用する
# 本番環境では設定しないこと
DEV_AUTH_ENABLED=true

# ==========================================================
# デプロイ設定（deploy.sh で使用）
# ==========================================================

# Lightsail インスタンスのホスト名または IP
LIGHTSAIL_HOST=your-lightsail-instance.example.com

# SSH ユーザー名（AlmaLinux のデフォルトは ec2-user）
LIGHTSAIL_USER=ec2-user

# SSH 秘密鍵のパス（省略可: 未設定時は SSH エージェントに委任）
# LIGHTSAIL_SSH_KEY=~/.ssh/ringiflow-demo.pem
