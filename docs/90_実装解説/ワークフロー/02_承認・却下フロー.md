# ワークフロー承認・却下フロー

対応 PR: #141, #479
対応 Issue: #75, #438

## 概要

承認者がワークフロー詳細画面から、割り当てられたステップに対して「承認」「却下」「差し戻し」のいずれかの操作を行う。多段階承認では、各ステップの承認者が順番に判断し、全ステップ承認で完了、いずれかで却下・差し戻しになるとワークフローが終了する。

## E2E フロー

### 正常系: 承認（中間ステップ）

```mermaid
sequenceDiagram
    participant U as ユーザー（承認者）
    participant Elm as Elm Frontend
    participant BFF as BFF
    participant Redis as Redis Session
    participant Core as Core Service
    participant DB as PostgreSQL
    participant Notify as 通知基盤

    U->>Elm: 「承認」ボタン → 確認ダイアログで確定

    Note over Elm, Redis: 1. BFF がセッション解決してリクエスト中継
    Elm->>BFF: POST /api/v1/workflows/{dn}/steps/{step_dn}/approve
    Note right of Elm: { version, comment }
    BFF->>Redis: セッション取得（Cookie → session_id）
    Redis-->>BFF: tenant_id, user_id
    BFF->>Core: POST /internal/.../approve + tenant_id, user_id

    Note over Core, DB: 2. 検証（同期）
    Core->>DB: SELECT workflow_steps (Active を確認)
    DB-->>Core: step row
    Note over Core: 権限チェック + 楽観的ロック
    Core->>DB: SELECT 次の workflow_step (Pending)
    DB-->>Core: next step row

    Note over Core, DB: 3. 永続化（トランザクション内、同期）
    Core->>DB: BEGIN
    Core->>DB: UPDATE workflow_steps（承認済み: Completed）
    Core->>DB: UPDATE workflow_steps（次ステップ: Active）
    Core->>DB: UPDATE workflow_instances（current_step_id 更新）
    Core->>DB: COMMIT
    DB-->>Core: OK

    Note over Core, Notify: 4. 通知（fire-and-forget）
    Core-)Notify: StepApproved → 申請者
    Core-)Notify: ApprovalRequest → 次の承認者

    Note over Core, Elm: 5. レスポンス
    Core-->>BFF: WorkflowInstanceDetailDto
    BFF-->>Elm: WorkflowInstance
    Elm->>U: ステップ承認完了、次の承認者に依頼中
```

### 正常系: 承認（最終ステップ）

```mermaid
sequenceDiagram
    participant U as ユーザー（承認者）
    participant Elm as Elm Frontend
    participant BFF as BFF
    participant Redis as Redis Session
    participant Core as Core Service
    participant DB as PostgreSQL
    participant Notify as 通知基盤

    U->>Elm: 「承認」ボタン → 確認ダイアログで確定

    Note over Elm, Redis: 1. BFF がセッション解決してリクエスト中継
    Elm->>BFF: POST /api/v1/workflows/{dn}/steps/{step_dn}/approve
    BFF->>Redis: セッション取得
    Redis-->>BFF: tenant_id, user_id
    BFF->>Core: POST /internal/.../approve + tenant_id, user_id

    Note over Core, DB: 2. 検証（同期）
    Core->>DB: SELECT workflow_steps (Active を確認)
    DB-->>Core: step row
    Note over Core: 権限チェック + 楽観的ロック

    Note over Core, DB: 3. 永続化（トランザクション内、同期）
    Core->>DB: BEGIN
    Core->>DB: UPDATE workflow_steps（承認済み: Completed）
    Core->>DB: UPDATE workflow_steps（残り Pending → Skipped）
    Core->>DB: UPDATE workflow_instances（status=Approved, completed_at）
    Core->>DB: COMMIT
    DB-->>Core: OK

    Note over Core, Notify: 4. 通知（fire-and-forget）
    Core-)Notify: Approved → 申請者

    Note over Core, Elm: 5. レスポンス
    Core-->>BFF: WorkflowInstanceDetailDto
    BFF-->>Elm: WorkflowInstance
    Elm->>U: ワークフロー承認完了
```

### 正常系: 却下

```mermaid
sequenceDiagram
    participant U as ユーザー（承認者）
    participant Elm as Elm Frontend
    participant BFF as BFF
    participant Redis as Redis Session
    participant Core as Core Service
    participant DB as PostgreSQL
    participant Notify as 通知基盤

    U->>Elm: 「却下」ボタン → 確認ダイアログで確定

    Note over Elm, Redis: 1. BFF がセッション解決してリクエスト中継
    Elm->>BFF: POST /api/v1/workflows/{dn}/steps/{step_dn}/reject
    BFF->>Redis: セッション取得
    Redis-->>BFF: tenant_id, user_id
    BFF->>Core: POST /internal/.../reject + tenant_id, user_id

    Note over Core, DB: 2. 検証（同期）
    Core->>DB: SELECT workflow_steps (Active を確認)
    DB-->>Core: step row
    Note over Core: 権限チェック + 楽観的ロック

    Note over Core, DB: 3. 永続化（トランザクション内、同期）
    Core->>DB: BEGIN
    Core->>DB: UPDATE workflow_steps（却下: Completed）
    Core->>DB: UPDATE workflow_steps（残り Pending → Skipped）
    Core->>DB: UPDATE workflow_instances（status=Rejected, completed_at）
    Core->>DB: COMMIT
    DB-->>Core: OK

    Note over Core, Notify: 4. 通知（fire-and-forget）
    Core-)Notify: Rejected → 申請者

    Note over Core, Elm: 5. レスポンス
    Core-->>BFF: WorkflowInstanceDetailDto
    BFF-->>Elm: WorkflowInstance
    Elm->>U: ワークフロー却下
```

### 正常系: 差し戻し

却下と同じ構造だが、ステップ遷移とインスタンス遷移が異なる:

- `step.request_changes(comment, now)` → Completed(RequestChanges)
- `instance.complete_with_request_changes(now)` → ChangesRequested
- 通知: ChangesRequested → 申請者

差し戻し後、申請者は再申請（resubmit）が可能。再申請時は ChangesRequested → InProgress に遷移する。

### 準正常系

| ケース | 検出箇所 | HTTP Status | ユーザーへの表示 |
|--------|---------|-------------|---------------|
| 権限なし（別の承認者に割当済み） | Core UseCase | 403 Forbidden | エラーメッセージ |
| ステップが Active 以外 | Core Domain | 400 Bad Request | エラーメッセージ |
| 楽観的ロック競合（他者が先に操作） | Repository | 409 Conflict | エラーメッセージ |
| ステップが見つからない | Repository | 404 Not Found | エラーメッセージ |

### Core Service 内部フロー（承認時）

E2E フローでは Core Service を 1 つの箱として扱っている。以下はその内部でリクエストがレイヤーを通過する流れ。

```mermaid
sequenceDiagram
    participant Handler as Core Handler
    participant UC as UseCase
    participant Domain as Domain Entity
    participant Repo as Repository
    participant DB as PostgreSQL

    Handler->>Repo: display_number → ID 解決
    Repo->>DB: SELECT workflow_steps WHERE display_number = $1
    DB-->>Repo: row
    Repo-->>Handler: WorkflowStepId

    Handler->>UC: approve_step(input, step_id, tenant_id, user_id)

    UC->>Repo: find_step(step_id)
    Repo->>DB: SELECT workflow_steps
    DB-->>Repo: row
    Repo-->>UC: WorkflowStep（Active 状態）

    Note over UC: 権限チェック: assigned_to == user_id
    Note over UC: 楽観的ロック: request.version == step.version

    UC->>Domain: step.approve(comment, now)
    Domain-->>UC: WorkflowStep（Completed/Approved）

    alt 中間ステップ
        UC->>Domain: instance.advance_to_next_step(next_step_id)
        UC->>Domain: next_step.activated(now)
    else 最終ステップ
        UC->>Domain: instance.complete_with_approval(now)
        UC->>Domain: 残り Pending ステップ.skipped(now)
    end

    UC->>Repo: save(tx, step, expected_version)
    Repo->>DB: UPDATE ... WHERE version = expected_version
    UC->>Repo: save(tx, instance, expected_version)
    Repo->>DB: UPDATE workflow_instances

    UC-->>Handler: WorkflowWithSteps
    Handler->>Repo: ユーザー名解決（user_id → 表示名）
    Repo->>DB: SELECT users
    DB-->>Repo: user rows
    Repo-->>Handler: ユーザー名
    Note over Handler: WorkflowWithSteps → WorkflowInstanceDetailDto
```

各レイヤーの責務:

| レイヤー | 責務 | 例 |
|---------|------|-----|
| Handler | HTTP → ドメイン型の変換、display_number の解決 | `Path(StepPathParams)` → `WorkflowStepId` |
| UseCase | ビジネスフローの制御、トランザクション管理 | 権限チェック → 状態遷移 → 保存 → 通知 |
| Domain | 状態遷移ルールの強制、不変条件の保護 | `step.approve()` は Active でのみ呼べる |
| Repository | 永続化、楽観的ロックの実施 | `WHERE version = expected_version` |

## コンポーネント間の境界

### API 契約

3つの操作は同じリクエスト/レスポンス型を共有する。

| エンドポイント | メソッド | 用途 |
|--------------|---------|------|
| `/api/v1/workflows/{dn}/steps/{step_dn}/approve` | POST | 承認 |
| `/api/v1/workflows/{dn}/steps/{step_dn}/reject` | POST | 却下 |
| `/api/v1/workflows/{dn}/steps/{step_dn}/request-changes` | POST | 差し戻し |

### 型変換の流れ

```
Elm ApproveRejectRequest { version: Int, comment: Maybe String }
  ↓ JSON encode
BFF ApproveRejectRequest { version: i64, comment: Option<String> }
  ↓ セッション情報を付加
Core ApproveRejectRequest { version, comment, tenant_id, user_id }
  ↓ Newtype 変換
Core UseCase ApproveRejectInput { version: Version, comment: Option<String> }
```

BFF がセッションから `tenant_id` と `user_id` を取得し、Core Service へのリクエストに付加する。フロントエンドは認証情報を意識しない。

### エラー伝播

| エラー | 発生箇所 | 伝播経路 | HTTP Status |
|--------|---------|---------|-------------|
| 権限なし | Core UseCase (`check_step_assigned_to`) | Core → BFF → Elm | 403 |
| 不正な状態遷移 | Domain (`step.approve()`) | DomainError → CoreError → BFF → Elm | 400 |
| 楽観的ロック競合 | Repository (`save()`) | InfraError → CoreError → BFF → Elm | 409 |
| ステップ不在 | Repository | InfraError → CoreError → BFF → Elm | 404 |

## 状態遷移

### ワークフローインスタンス

```mermaid
stateDiagram-v2
    [*] --> Draft: 下書き保存
    Draft --> InProgress: 申請
    InProgress --> InProgress: 中間ステップ承認（advance_to_next_step）
    InProgress --> Approved: 最終ステップ承認（complete_with_approval）
    InProgress --> Rejected: 却下（complete_with_rejection）
    InProgress --> ChangesRequested: 差し戻し（complete_with_request_changes）
    ChangesRequested --> InProgress: 再申請（resubmitted）
    InProgress --> Cancelled: 取り消し
```

### 承認ステップ

```mermaid
stateDiagram-v2
    [*] --> Pending: 申請時に生成
    Pending --> Active: 前ステップ承認 or 最初のステップ
    Active --> Completed: approve / reject / request_changes
    Pending --> Skipped: ワークフロー終了時（却下・差し戻し・最終承認）

    state Completed {
        [*] --> Approved
        [*] --> Rejected
        [*] --> RequestChanges
    }
```

型安全ステートマシンにより、各状態で有効なフィールドのみを保持する:

- `Active`: `started_at` を持つ
- `Completed`: `decision`, `comment`, `started_at`, `completed_at` を持つ
- `Pending`, `Skipped`: 追加フィールドなし

## 設計判断

### 1. 承認・却下・差し戻しで API を分けるか

3つの操作を別エンドポイントとして提供している。

| 案 | 明確さ | API 数 | リクエスト型の統一性 |
|----|-------|--------|-------------------|
| **エンドポイント分離（採用）** | 操作の意図が URL で明確 | 3 エンドポイント | 同一型を共有 |
| 単一エンドポイント + action パラメータ | URL からは操作が不明 | 1 エンドポイント | action フィールドが必要 |

採用理由: RESTful な設計として操作の意図が URL に表れる方が明確。リクエスト/レスポンス型は同一（`ApproveRejectRequest`）なので、型の重複はない。

### 2. 却下・差し戻しの共通化

却下と差し戻しは「ワークフローを終了する」という共通の振る舞いを持つため、`terminate_step()` として共通化し、`StepTerminationType` enum で分岐する。

| 共通処理 | 差異 |
|---------|------|
| 権限チェック、楽観的ロック | ドメインメソッド（`reject` / `request_changes`） |
| 残ステップの Skipped 化 | インスタンス遷移先（`Rejected` / `ChangesRequested`） |
| トランザクション内保存 | 通知種別（`Rejected` / `ChangesRequested`） |

### 3. 通知の発火タイミング

通知はトランザクションコミット後に fire-and-forget で送信する。通知失敗がワークフロー操作をブロックしない設計。

## 関連ドキュメント

- [ワークフロー申請フロー](01_申請フロー.md)
- [詳細設計書: ワークフロー承認却下機能設計](../../40_詳細設計書/11_ワークフロー承認却下機能設計.md)
